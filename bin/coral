#!/bin/sh

. $(coral_bootstrap)
require "coral-core"

if test $# -eq 0; then
	# display_help
  echo "help goes here"
else
	while test $# -ne 0; do
		case $1 in
      install) coral-core command_install ${@:2} ;;
      init) coral-core command_init ;;
		esac
		shift
	done
fi
